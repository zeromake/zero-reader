import { h, render, Component, findDOMNode } from "zreact";
import Router, { Route, Link } from "preact-router";

export {
    h,
    Component,
    findDOMNode,
    render,
    Router,
    Route,
    Link,
};
