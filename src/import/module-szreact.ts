import { createElement as h, Component, cloneElement, Children } from "@zeromake/zreact";
import { render, findDOMNode, unmountComponentAtNode } from "@zeromake/zreact";

export {
    h,
    Component,
    findDOMNode,
    render,
    cloneElement,
    Children,
    unmountComponentAtNode,
};
